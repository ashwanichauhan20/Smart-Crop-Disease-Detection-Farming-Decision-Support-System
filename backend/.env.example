# MongoDB Connection String
# Replace with your actual MongoDB Atlas connection string
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/farmdb?retryWrites=true&w=majority

# Server Port (use 5001 to avoid conflicts with frontend dev server on 5173)
PORT=5001

# Node Environment
NODE_ENV=development
